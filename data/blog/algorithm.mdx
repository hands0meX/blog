---
title: 算法日常
date: '2024-03-07'
tags: ['algorithm']
draft: false
summary: 算法
---
## 复杂度分析
### 时间复杂度
O(1) < O($\log(n)$) < O(n) < O(n$\log(n)$) < O($n^2$) < O($2^n$)
### 空间复杂度分析
> 递归算法会 *同时* 开辟栈帧空间,所有的函数空间内存同时存在， 帧栈空间通常只在递归函数影响空间复杂度。

## 数据结构

> 所有的数据结构都是基于数组,链表 或者其组合实现的。

## 分治
### 分治算法
> 先微分再积分